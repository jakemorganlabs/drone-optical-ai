# fill at runtime by wizard
# interface=        # set by wizard (e.g. wlan0)
# dhcp-range=       # set by wizard (e.g. 10.0.0.10,10.0.0.50,12h)
# dhcp-option=option:router,10.0.0.1
# dhcp-option=option:dns-server,10.0.0.1
# address=/#/10.0.0.1
# no-resolv
# log-dhcp